"""« Données »: export, import and clear the configuration and the data.

One page (`/donnees/`) replaced « Exporter / Importer les associations ».
What depends on what is one table (`registry.INFO`); each checkbox is a
section class (`sections/`); the harness around them (`runner`) makes the
preview the real run rolled back, and `safety` backs everything up before a
confirmed run.
"""
